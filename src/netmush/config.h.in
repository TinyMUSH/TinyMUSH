/**
 * @file config.h
 * @author TinyMUSH development team (https://github.com/TinyMUSH)
 * @brief Configuration options set by configure script
 * @version 4.0
 *
 * @copyright Copyright (C) 1989-2025 TinyMUSH development team.
 *            You may distribute under the terms the Artistic License,
 *            as specified in the COPYING file.
 *
 * @note Do not edit this file by hand -- Your changes will be overwritten
 *
 */

#ifndef __CONFIG_H
#define __CONFIG_H

#define _GNU_SOURCE

/* Maximum length of a delimiter */
#define MAX_DELIM_LEN ${MAX_DELIM_LEN}

/* max # of iter levels */
#define MAX_ITER_NESTING ${MAX_ITER_NESTING}

/* Number of env vars (%0 et al) */
#define NUM_ENV_VARS ${NUM_ENV_VARS}

/* Maximum parse depth to prevent stack overflow */
#define MAX_BOOLEXP_PARSE_DEPTH ${MAX_BOOLEXP_PARSE_DEPTH}

/* Define to the version broken down to it's components */
#define TINYMUSH_VERSION_MAJOR ${PROJECT_VERSION_MAJOR}
#define TINYMUSH_VERSION_MINOR ${PROJECT_VERSION_MINOR}
#define TINYMUSH_VERSION_PATCH ${PROJECT_VERSION_PATCH}
#define TINYMUSH_VERSION_TWEAK ${PROJECT_VERSION_TWEAK}

/* Define to the release status */
#define TINYMUSH_RELEASE_STATUS ${TINYMUSH_RELEASE_STATUS}

/* Define to the version of this package. */
#define TINYMUSH_GIT_VERSION "${TINYMUSH_GIT_VERSION}"

/* Define to the last commit date */
#define TINYMUSH_GIT_RELEASE_DATE "${TINYMUSH_GIT_RELEASE_DATE}"

/* Define to the git hash of the last commit. */
#define TINYMUSH_GIT_HASH "${TINYMUSH_GIT_HASH}"

/* 1 if source was modified since the last commit, else 0. */
#define TINYMUSH_GIT_DIRTY ${TINYMUSH_GIT_DIRTY}

/* Define to the build date*/
#define TINYMUSH_BUILD_DATE "${TINYMUSH_BUILD_DATE}"

/* Define to the authors*/
#define TINYMUSH_AUTHOR "${TINYMUSH_AUTHOR}"

/* Define to the email contact*/
#define TINYMUSH_CONTACT "${TINYMUSH_CONTACT}"

/* Define to the project copyright*/
#define TINYMUSH_COPYRIGHT "${TINYMUSH_COPYRIGHT}"

/* Define to the project license*/
#define TINYMUSH_LICENSE "${TINYMUSH_LICENSE}"

/* Define to the homepage url*/
#define TINYMUSH_HOMEPAGE_URL "${PROJECT_HOMEPAGE_URL}"

#define DEFAULT_SHORTNAME "netmush"

#define DEFAULT_BACKUP_UTIL "${TAR_BINARY}"
#define DEFAULT_BACKUP_COMPRESS "czvf"
#define DEFAULT_BACKUP_EXTRACT "xzvf"
#define DEFAULT_BACKUP_EXT "tbz"

#define DEFAULT_BACKUP_HOME "${TINYMUSH_ROOT_DIRECTORY}/backups"
#define DEFAULT_BINARY_HOME "${TINYMUSH_ROOT_DIRECTORY}"
#define DEFAULT_CONFIG_FILE "${TINYMUSH_ROOT_DIRECTORY}/configs/netmush.conf"
#define DEFAULT_CONFIG_HOME "${TINYMUSH_ROOT_DIRECTORY}/configs"
#define DEFAULT_DATABASE_HOME "${TINYMUSH_ROOT_DIRECTORY}/db"
#define DEFAULT_LOG_HOME "${TINYMUSH_ROOT_DIRECTORY}/logs"
#define DEFAULT_MODULES_HOME "${TINYMUSH_ROOT_DIRECTORY}/modules"
#define DEFAULT_PID_HOME "${TINYMUSH_ROOT_DIRECTORY}"
#define DEFAULT_SCRIPTS_HOME "${TINYMUSH_ROOT_DIRECTORY}/scripts"
#define DEFAULT_TEXT_HOME "${TINYMUSH_ROOT_DIRECTORY}/docs"

#endif /* __CONFIG_H */
