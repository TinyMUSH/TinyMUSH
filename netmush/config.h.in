/**
 * @file config.h
 * @author TinyMUSH development team (https://github.com/TinyMUSH)
 * @brief Configuration options set by configure script
 * @version 4.0
 * @date 2023-11-26
 *
 * @copyright Copyright (C) 1989-2023 TinyMUSH development team.
 *            You may distribute under the terms the Artistic License,
 *            as specified in the COPYING file.
 *
 * @note Do not edit this file by hand -- Your changes will be overwritten
 *
 */

#ifndef __CONFIG_H
#define __CONFIG_H

#define _GNU_SOURCE

/* Maximum length of a delimiter */
#define MAX_DELIM_LEN ${MAX_DELIM_LEN}

/* max # of iter levels */
#define MAX_ITER_NESTING ${MAX_ITER_NESTING}

/* Number of env vars (%0 et al) */
#define NUM_ENV_VARS ${NUM_ENV_VARS}

/* Name of package */
#define PACKAGE "${PACKAGE}"

/* Define to the address where bug reports for this package should be sent. */
#define PACKAGE_BUGREPORT "${PROJECT_HOMEPAGE_URL}TinyMUSH/issues"

/* Copyright Notice */
#define PACKAGE_COPYRIGHT "Copyright (C) 1989-2023 TinyMUSH development team."

/* Define to the full name of this package. */
#define PACKAGE_NAME "${PROJECT_NAME}"

/* Date of Release */
#define PACKAGE_RELEASE_DATE "${PACKAGE_RELEASE_DATE}"

/* Define to the full name and version of this package. */
#define PACKAGE_STRING "${PROJECT_NAME} ${PROJECT_VERSION}"

/* Define to the one symbol short name of this package. */
#define PACKAGE_TARNAME "${PACKAGE}"

/* Define to the home page for this package. */
#define PACKAGE_URL "${PROJECT_HOMEPAGE_URL}"

/* Define to the version of this package. */
#define PACKAGE_VERSION "${PROJECT_VERSION}"

#define DEFAULT_SHORTNAME "netmush"

#define DEFAULT_BACKUP_UTIL "${TAR_BINARY}"
#define DEFAULT_BACKUP_COMPRESS "czvf"
#define DEFAULT_BACKUP_EXTRACT "xzvf"
#define DEFAULT_BACKUP_EXT "tbz"

#define DEFAULT_BACKUP_HOME "${PROJECT_SOURCE_DIR}/game/backups"
#define DEFAULT_BINARY_HOME "${PROJECT_SOURCE_DIR}/game"
#define DEFAULT_CONFIG_FILE "${PROJECT_SOURCE_DIR}/game/configs/netmush.conf"
#define DEFAULT_CONFIG_HOME "${PROJECT_SOURCE_DIR}/game/configs"
#define DEFAULT_DATABASE_HOME "${PROJECT_SOURCE_DIR}/game/db"
#define DEFAULT_LOG_HOME "${PROJECT_SOURCE_DIR}/game/logs"
#define DEFAULT_MODULES_HOME "${PROJECT_SOURCE_DIR}/game/modules"
#define DEFAULT_PID_HOME "${PROJECT_SOURCE_DIR}/game"
#define DEFAULT_SCRIPTS_HOME "${PROJECT_SOURCE_DIR}/game/scripts"
#define DEFAULT_TEXT_HOME "${PROJECT_SOURCE_DIR}/game/docs"

#endif /* __CONFIG_H */
