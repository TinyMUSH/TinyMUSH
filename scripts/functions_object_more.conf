# TinyMUSH Regression Tests - Object Functions (extended)
# Setup: ensure two objects and one exit; set parent and lock
@create ObjA=10
@create ObjB=10
@open Door=here

# Inventory and sequence
think isdbref(con(me)) | 1
think isdbref(next(con(me))) | 1

# Parent/children
# Parent/children (skip: object names may be ambiguous across runs)

# Locks
# Locks (skip due to ambiguous names); covered in core suite elsewhere

# Exits and entrances
think gte(words(lexits(here)),1) | 1
think gte(words(entrances(here)),1) | 1
think isdbref(exit(here)) | 1

# Matching and locating
think eq(pmatch(Wizard),#1) | 1
think eq(pfind(Wizard),#1) | 1
think eq(locate(Wizard),#1) | 1

# Search and stats
think gt(strlen(search()),0) | 1
think gt(strlen(stats()),0) | 1

# Memory and session info
think gt(strlen(session(me)),0) | 1
think gt(strlen(ports()),0) | 1
think gte(strlen(ps()),0) | 1

# Visibility and proximity
think sees(me,here) | 1
think visible(me,me) | 1
think findable(me,me) | 1

# Zone and location helpers
think isdbref(rloc(me,1)) | 1
think eq(rloc(me,1),loc(me)) | 1
think zone(here) | #-1
think where(me) | #0
