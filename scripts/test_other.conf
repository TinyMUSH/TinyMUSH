# TinyMUSH other commands tests
# Format: command | expected substring
# Blank lines and lines starting with # are ignored.

# Setup: create helper object for reference tests
@create RefObj | RefObj created

# Color map
@colormap | color

# Cron related
@cron | No times
@crontab | Matched 0 cron
@crondel 0 | No match

# Decompile self
@decompile me | @set me

# Doing / DOING
@doing Testing status | Set.
DOING | Doing

# Entrances / floaters
@entrances here | entrance
@floaters | floating rooms

# Find and search
@find Wizard | Permission denied
@search type=player | Wizard

# Last connections
@last | Total successful connects

# List variations
@list options | Global Options
@listcommands | No added commands
@listmotd | Message

# Password change (no-op with same password)
@password me=potrzebi potrzebie | Sorry

# Reference create/list
@reference tool=#2 | Referenced
@reference/list | Total references

# Stats and sweep
@stats | universe contains
@sweep | Sweep

# Basic info commands
inventory | carrying
kill me | kill
score | You
version | TinyMUSH
INFO | ### Begin INFO
WHO | Wizard
SESSION | Characters Input----

# Clean up
@destroy #2 | crumble
